#include <stdio.h>
/**
 * main - Printing alphabets in lowercase and in reverse
 *
 * Return: Always 0
 */
int main(void)
{
	char p;

	p = 'z';
	while
		(p >= 'a')
		{
			putchar(p);
			p--;
		}
	putchar('\n');
	return (0);
}
