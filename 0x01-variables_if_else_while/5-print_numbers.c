#include <stdio.h>

/**
 * main - Program prints all single digits of base 10 starting from number 0
 * Return: Always 0
 */
int main(void)
{
	int y;

	for (y = 0; y < 10; y++)
		printf("%d", y);
	putchar('\n');
	return (0);
}
